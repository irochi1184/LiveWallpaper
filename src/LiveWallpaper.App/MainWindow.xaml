<Window
    x:Class="LiveWallpaper.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="32">
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="16"
            MaxWidth="520">
            <TextBlock
                Text="LiveWallpaper"
                FontSize="32"
                FontWeight="SemiBold" />
            <TextBlock
                Text="最初の技術検証：デスクトップ背景に秒付き時計を表示します。"
                TextWrapping="Wrap"
                Opacity="0.72" />
            <Button
                Content="時計をデスクトップに表示"
                Click="ShowWallpaperButton_Click"
                HorizontalAlignment="Stretch" />
            <Button
                Content="時計を閉じる"
                Click="CloseWallpaperButton_Click"
                HorizontalAlignment="Stretch" />
            <TextBlock
                x:Name="StatusText"
                Text="未開始"
                TextWrapping="Wrap"
                Opacity="0.72" />
        </StackPanel>
    </Grid>
</Window>
